name: Benchmark a Pull Request

# What this does and does not cover: GitHub-hosted runners have no GPU, so this compares the
# host-side construction path only — matrix assembly, CSC→CSR transpose, ghost discovery and
# topology, column remapping (see benchmark/benchmarks.jl). Device timings live in
# benchmark/gpu.jl and are run by hand on GPU hardware. A clean table here says nothing about
# multi-GPU performance.

# No branches filter: stacked PRs target other feature branches, and each PR is benchmarked
# against its own base (base.sha), not the default branch.
on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v7
        with:
          # benchpkg resolves both PR revisions out of the local clone (--path=.), so it needs
          # full history rather than the default shallow fetch.
          fetch-depth: 0
      - uses: julia-actions/setup-julia@v3
        with:
          version: "1.12"
      - uses: julia-actions/cache@v3
      - name: Extract Package Name from Project.toml
        id: extract-package-name
        run: |
          PACKAGE_NAME=$(grep "^name" Project.toml | sed 's/^name = "\(.*\)"$/\1/')
          echo "package_name=$PACKAGE_NAME" >> $GITHUB_OUTPUT
      - name: Build AirspeedVelocity
        env:
          JULIA_NUM_THREADS: 2
        run: |
          # Lightweight build step, as sometimes the runner runs out of memory:
          julia -e 'ENV["JULIA_PKG_PRECOMPILE_AUTO"]=0; import Pkg; Pkg.add(;url="https://github.com/MilesCranmer/AirspeedVelocity.jl.git")'
          julia -e 'ENV["JULIA_PKG_PRECOMPILE_AUTO"]=0; import Pkg; Pkg.build("AirspeedVelocity")'
      - name: Add ~/.julia/bin to PATH
        run: |
          echo "$HOME/.julia/bin" >> $GITHUB_PATH
      - name: Run benchmarks
        run: |
          mkdir results
          benchpkg ${{ steps.extract-package-name.outputs.package_name }} --rev="${{ github.event.pull_request.base.sha }},${{ github.event.pull_request.head.sha }}" --path=. --bench-on="${{ github.event.pull_request.head.sha }}" --output-dir=results/ --tune
      - name: Create plots from benchmarks
        run: |
          mkdir -p plots
          benchpkgplot ${{ steps.extract-package-name.outputs.package_name }} --rev="${{ github.event.pull_request.base.sha }},${{ github.event.pull_request.head.sha }}" --npart=10 --format=png --input-dir=results/ --output-dir=plots/
      - name: Upload plot as artifact
        uses: actions/upload-artifact@v7
        with:
          name: plots
          path: plots
      - name: Create markdown table from benchmarks
        run: |
          benchpkgtable ${{ steps.extract-package-name.outputs.package_name }} --rev="${{ github.event.pull_request.base.sha }},${{ github.event.pull_request.head.sha }}" --input-dir=results/ --ratio > table.md
          echo '### Benchmark Results' > body.md
          echo '' >> body.md
          echo 'Host-side construction path only — the runner has no GPU. Device timings come from `benchmark/gpu.jl`, run manually on GPU hardware.' >> body.md
          echo '' >> body.md
          cat table.md >> body.md
          echo '' >> body.md
          echo '' >> body.md
          echo '### Benchmark Plots' >> body.md
          echo 'A plot of the benchmark results have been uploaded as an artifact to the workflow run for this PR.' >> body.md
          echo 'Go to "Actions"->"Benchmark a pull request"->[the most recent run]->"Artifacts" (at the bottom).' >> body.md
      - name: Find Comment
        uses: peter-evans/find-comment@v4
        id: fcbenchmark
        with:
          issue-number: ${{ github.event.pull_request.number }}
          comment-author: "github-actions[bot]"
          body-includes: Benchmark Results
      - name: Comment on PR
        uses: peter-evans/create-or-update-comment@v5
        with:
          comment-id: ${{ steps.fcbenchmark.outputs.comment-id }}
          issue-number: ${{ github.event.pull_request.number }}
          body-path: body.md
          edit-mode: replace
